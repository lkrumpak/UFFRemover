(function(){{
    'production' !== 'development' ? warning(false, 'Don\'t set the %s property of the React element. Instead, ' + 'specify the correct value when initially creating the element.', key) : null;
        $that._store[key] = value;
}})();