(function(){{
    if (b === n) {
        var d =             $that.v(), c = d.slice(0);
                $that.X();
        d.splice(0, d.length);
                $that.W();
        return c;
    }
    return b ?     $that.remove(function (c) {
        return 0 <= a.a.m(b, c);
    }) : [];
}})();