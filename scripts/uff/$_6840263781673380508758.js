(function(){{
    if (a instanceof mc && a.constructor === mc && a.Cc === nc)
        return a.Bb;
    Ha('expected object of type TrustedResourceUrl, got \'' + a + '\'');
    return 'type_error:TrustedResourceUrl';
}})();