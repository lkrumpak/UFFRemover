(function(){{
    if (        $that.hash) {
                $that.hashes.push(        $that.hash);
    }
        $that.hash = {
        values: [],
        types: [],
        contexts: []
    };
}})();