(function(){{
    // Render this with template provided.
    // Note that unlike backbone this includes the root element.
        $that.renderWithTemplate();
    // cache reference to `input` for speed/convenience
        $that.input =     $that.queryByHook('input');
}})();