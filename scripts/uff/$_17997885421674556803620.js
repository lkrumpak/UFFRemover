(function(){{
    LocationInfo.call(    $that, locInfo);
        $that.type = 'partial';
        $that.partialName = partialName;
        $that.context = context;
        $that.hash = hash;
        $that.strip = strip;
        $that.strip.inlineStandalone = true;
}})();