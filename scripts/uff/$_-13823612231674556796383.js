(function(){{
        $that.pushStackLiteral('{}');
    if (        $that.options.stringParams) {
                $that.push('{}');
        // hashContexts
                $that.push('{}');    // hashTypes
    }
}})();