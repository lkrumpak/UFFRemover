(function(){{
    e = '' + e;
    var i = e.length;
    if (i < t) {
        var s = [];
        for (var o = t - i; o > 0; o -= 1)
            s.push(n);
        r === !0 ? e = s.join('') + e : e += s.join('');
    }
    return e;
}})();