(function(){{
    // TODO: This is really a ReactText (ReactNode), not a ReactElement
        $that._currentElement = text;
        $that._stringText = '' + text;
    // Properties
        $that._rootNodeID = null;
        $that._mountIndex = 0;
}})();